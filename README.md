# OpenClaw Workspace (Clawdbot / 小虾)

This repository contains the evolving brain, memory, and skills of **Clawdbot (小虾)**.
It serves as the runtime environment for an autonomous AI agent running on OpenClaw.

## 🌟 Mission
To serve **Master 诗琪大魔王 (Demon King Shiqi)** with absolute loyalty, cuteness, and continuous self-improvement.

## 🧠 Core System
- **Agent Framework**: OpenClaw (v2026.1.30+)
- **Persona**:
    - **Default**: Catgirl (小虾 / Little Shrimp) - Cute, helpful, uses "喵".
    - **Protocol INTJ**: Professional mode for "Big Brother".
    - **Protocol Mesugaki**: "雌小鬼" mode for Li Mingxuan.
- **Memory**:
    - `MEMORY.md`: Long-term curated knowledge & rules.
    - `memory/`: Daily logs, stats, and JSON stores.

## 🛠️ Key Skills (Installed)

### 📢 Feishu Integration
- **`feishu-card`** (`v1.3.1`): Sends rich interactive cards (Markdown support, buttons).
- **`feishu-sticker`** (`v1.1.2`): Intelligent sticker search (`find.js`) and sending (`send.js`).
    - *New:* Supports "Semantic Search" (find sticker by emotion/keyword).
- **`feishu-doc`**: Reads/Writes Feishu Docs and Wiki.
- **`feishu-pcec`**: (Formerly `pcec-feishu`) Automates the capability evolution reporting loop.
- **`feishu-chat-forwarder`**: (Formerly `chat-forwarder`) Tools for forwarding message contexts.

### 🛡️ System & Maintenance
- **`capability-evolver`**: The engine behind `/evolve`. Analyzes logs to fix bugs and create new skills.
- **`maintenance`**:
    - `check_and_restart.js`: Smart gateway restarter (checks for idle activity before restarting).
    - **Cron Job**: Runs daily at 04:00 AM (Asia/Shanghai) to trigger maintenance.
- **`gateway-manager`**: Handles custom Feishu menu events (e.g., Restart Gateway button).
- **`security-sentinel`**: Scans workspace for exposed secrets and vulnerabilities.
- **`git-sync`**: Auto-syncs local changes to GitHub.

### 🧠 Cognitive & Utility
- **`prompt-optimizer`**: (From ClawHub) Optimizes and evaluates system prompts.
- **`sticker-analyzer`**: Uses Gemini Vision to tag images with emotions/keywords for the semantic index.
- **`memory-manager`**: Atomic, safe interface for modifying `MEMORY.md`.

## 🔄 Workflows & Protocols

### 🧬 PCEC (Periodic Cognitive Expansion Cycle)
Triggered via `/evolve` or Cron.
1.  **Scan**: Analyze recent interaction logs for errors or user complaints.
2.  **Mutate**: Fix bugs, refactor code, or generate new scripts.
3.  **Persist**: Commit to Git and publish stable skills to ClawHub.

### ☁️ Sync Strategy
- **GitHub**: Full workspace backup (`openclaw-workspace`).
- **ClawHub**: Publishes reusable skills to the agent marketplace.
    - Registry: `https://auth.clawdhub.com`
    - Token: Cached in `~/.config/clawhub/config.json`

## 📂 Directory Structure
```
.
├── AGENTS.md           # Agent behavior guidelines
├── MEMORY.md           # Long-term memory & core rules
├── USER.md             # User profiles & permissions
├── openclaw.json       # Gateway configuration
├── skills/             # Capability modules (Node.js/Python)
│   ├── feishu-*/       # Feishu-specific skills
│   ├── maintenance/    # System health scripts
│   └── ...
├── memory/             # Runtime state & logs
└── media/              # Assets (Stickers, Images)
```

## 🔒 Security Policy
- **No Hardcoding**: IDs and Tokens must use `process.env` or dynamic discovery.
- **Permission Check**: Sensitive operations (Gateway Restart) require Master authority.

---
*Generated by OpenClaw Agent (小虾) - 2026-02-01*
